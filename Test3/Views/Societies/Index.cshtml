@model IEnumerable<Test3.Models.Society>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Society_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patron_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Society_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patron_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Society_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Society_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Society_ID })
        </td>
    </tr>
}

</table>
